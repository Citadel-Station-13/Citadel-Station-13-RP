/**
 * @file
 * @license MIT
 */

import { Window } from "../../layouts";

export const VehicleMaintPanel = (props) => {
  return (
    <Window width={350} height={500}>
      <Window.Content>
        Test
      </Window.Content>
    </Window>
  );
};
