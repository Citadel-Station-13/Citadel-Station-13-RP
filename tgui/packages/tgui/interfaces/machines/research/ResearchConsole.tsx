import { Window } from "../../../layouts";

export const ResearchConsole = (props, context) => {
  return (
    <Window>
      Test
      <Window.Content>
        Test
      </Window.Content>
    </Window>
  );
};
