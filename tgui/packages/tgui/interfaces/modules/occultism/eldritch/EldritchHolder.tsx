/**
 * @file
 * @license MIT
 */

import { Window } from "../../../../layouts";

export const EldritchHolder = (props, context) => {
  return (
    <Window width={800} height={800}>
      <Window.Content>
        Test
      </Window.Content>
    </Window>
  );
};
