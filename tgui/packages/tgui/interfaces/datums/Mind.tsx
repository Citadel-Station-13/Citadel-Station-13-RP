/**
 * @file
 * @license MIT
 */

import { Window } from "../../layouts";

export const Mind = (props, context) => {
  return (
    <Window title={`Recollection - ${data.realName}`}>
      <Window.Content>
        test
      </Window.Content>
    </Window>
  );
};
