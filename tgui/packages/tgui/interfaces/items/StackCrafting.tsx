import { Window } from "../../layouts";

export const StackCrafting = (props, context) => {
  return (
    <Window width={350} height={350}>
      <Window.Content>
        Testing
      </Window.Content>
    </Window>
  );
};
