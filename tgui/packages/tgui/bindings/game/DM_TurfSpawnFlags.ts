/**
 * @file
 * @license MIT
 */

export enum DM_TurfSpawnFlags {
  AllowFill = (1<<0),
  AllowBuildmode = (1<<1),
  AllowHolodeck = (1<<2),
  AllowVirtualReality = (1<<3),
  AllowLevelBaseturf = (1<<4)
}
