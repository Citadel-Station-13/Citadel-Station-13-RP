/**
 * turns out, we really don't want to make a /datum/species subspecies for everyone's snowflakes
 *
 * but also, vars[] access is banned (because it's awful) but hey, we need a way to modify
 * species datums, without taking too long to do it!
 *
 * therefore, introducing **templates.**
 */
/datum/character_species
