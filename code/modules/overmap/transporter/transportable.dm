/**
 * simple objects, allows someone to transport us to spawn on the pad, being semantically deleted in the process
 * basically, loot.
 */
/atom/movable/overmap_object/entity/transportable
#warn impl
