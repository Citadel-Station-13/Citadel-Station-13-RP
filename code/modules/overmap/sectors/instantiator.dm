/**
 * eventually we'll have proper maploading + shuttles
 * and instantiation will automatically be done that way
 *
 * for now, we use instantiator landmarks.
 */
/obj/landmark/overmap_entity_instantiator
	/// name of entity
	var/name = "unknown entity"
	/// desc of entity
	var/desc = "What is this?"
