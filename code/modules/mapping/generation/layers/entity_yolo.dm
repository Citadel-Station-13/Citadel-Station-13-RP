/**
 * default implementation of "just throw structures + objects + mobs around haphazardly based on biome"
 * pretty close to how seed_submaps() works/worked (depending on when you're reading this).
 */
/datum/map_layer/entity/yolo

#warn impl all
