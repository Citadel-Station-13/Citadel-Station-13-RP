/**
 * ! Unique Generic Objects
 *
 * used for storing objects created at runtime
 * objects are stored by coordinates, map, and data
 *
 * use for things like persistent player-made structures
 */
/datum/controller/subsystem/persistence

/datum/controller/subsystem/persistence/proc/LoadPersistentAtoms()

/datum/controller/subsystem/persistence/proc/SavePersistentAtoms()
