/datum/geometry

/**
 * checks non-metadata (aka geometric/locational) equality
 * other MUST be the same type as us!
 */
/datum/geometry/proc/equals(datum/geometry/other)
	CRASH("not implemented")

/**
 * get volume
 */
/datum/geometry/proc/volume()
	CRASH("not implemented")
