//! /atom rad_flags
/// block to/from contents
#define RAD_BLOCK_CONTENTS (1<<0)
/// do not contaminate
#define RAD_NO_CONTAMINATE (1<<1)
/// registered as a z listener
#define RAD_Z_LISTENING (1<<23)
