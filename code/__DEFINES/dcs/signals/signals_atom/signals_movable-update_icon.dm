/**
 * Anything relating to update icon/rendering
 */

/// Called at the base of update_transform with the matrix that's being applied.
/// * Signature: (matrix/applying)
#define COMSIG_MOVABLE_UPDATE_TRANSFORM "movable-update_transform"
